#!/usr/bin/make -f

# This file was automatically generated by stdeb 0.8.2 at
# Tue, 03 Nov 2015 15:43:28 +1100
export PYBUILD_NAME=cramsclient-nectar
%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
    commands = nosetests

# Needed as clean calls python setup.py clean which needs pip but pip isn't installed yet
# Think this is a bug with python3
override_dh_auto_clean:
